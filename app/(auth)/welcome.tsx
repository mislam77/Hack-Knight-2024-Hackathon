import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Signup = () => {
  return (
    <SafeAreaView className="flex-1 justify-center items-center w-full min-h-[85vh]">
      <Text className="text-center font-popbold">Okay</Text>
    </SafeAreaView>
  );
};

export default Signup;
